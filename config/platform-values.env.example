# Public-safe default values for local Jenkins runtime contract validation.
# Copy this file to an ignored environment-specific config/platform-values*.env
# file before adding private or deployment-specific values.

PLATFORM_ENVIRONMENT=example
PLATFORM_DOMAIN=example.invalid
PLATFORM_ENABLE_DEPLOYMENT=false
PLATFORM_DOCKER_REGISTRY=
